#include "main.h"
/**
 *main - entry
 *checks for uppercase character
 *Returns 1 if c is uppercase, zero otherwise
 */
int main(void)
{
	int _isupper(int c)
	{
		if (c = 'A' <= 'Z')
		{
			return (1);
		}
		else
			return (0);
	}
}
